===== Name
cljs.core/object?

===== Signature
[x]

===== Description

Returns true if `x` is a JavaScript object, false otherwise.

===== Example

```
(object? (js/Object))
;; => true

(object? {:key "value"})
;; => false
```

===== Related
cljs.core/array?
