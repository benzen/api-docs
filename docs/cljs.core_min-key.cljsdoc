===== Name
cljs.core/min-key

===== Signature
[k x]
[k x y]
[k x y & more]

===== Description

Returns the `x` for which `(k x)` is least.

`(k x)` should return a number.

===== Example#8a838d

```
(min-key :key {:key 10} {:key 20})
;; => {:key 10}
```

===== Related
cljs.core/min
cljs.core/max-key
