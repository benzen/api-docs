===== Name
cljs.core/find

===== Signature
[coll k]

===== Description

Returns the map entry for key `k`, or nil if `k` is not found.

===== Example#6ef9fd

```
(find {:key "value" :k "v"} :k)
;; => [:k "v"]
```

===== TODO
The description says a map entry.
But running the example give a vector.
Is this the expected behavior ?

===== Related
cljs.core/get
cljs.core/get-in
