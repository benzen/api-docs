===== Name
cljs.core/odd?

===== Signature
[n]

===== Description

Returns true if `n` is an odd number.

Throws an exception if `n` is not an integer.

===== Example

```
(even? 1)
;; => false

(even? 2)
;; => true

(even? true)
;; => Error: Argument must be an integer: true
```

===== Related
cljs.core/even?
