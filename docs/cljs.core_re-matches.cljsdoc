===== Name
cljs.core/re-matches

===== Signature
[re s]

===== Description

Returns the result of `(re-find re s)` if `re` fully matches `s`.

===== Example

```
(re-matches #"123" "12345")
;; => nil

(re-matches #"123" "123")
;; => "123"
```

===== Related
re-find
re-pattern
re-seq
