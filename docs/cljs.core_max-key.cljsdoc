===== Name
cljs.core/max-key

===== Signature
[k x]
[k x y]
[k x y & more]

===== Description

Returns the `x` for which `(k x)` is greatest.

`(k x)` should return a number.

===== Example

```
(max-key :key {:key 123} {:key 345})
;; => {:key 345}

(max-key :key {:key "abc"} {:key "abd"} {:key "a"})
;; => {:key "abd"}

```

===== Related
cljs.core/max
cljs.core/min-key
