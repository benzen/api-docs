===== Name
cljs.core/select-keys

===== Signature
[map keys]

===== Description

Returns a map containing only those entries in `map` whose key is in `keys`.

===== Example

```
 (select-keys {:k1 "value" :k2 "value2"} [:k1])
 ;; => {:k1 "value"}
```

===== Related
