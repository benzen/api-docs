===== Name
cljs.core/not-every?

===== Signature
[pred coll]

===== Description

Returns false if `(pred x)` is logical true for every `x` in `coll`, else true.

===== Example

```
(not-every? even? [1 2 3 4])
;; => true

(not-every? odd? [1 3 5 7])
;; => false
```

===== Related
cljs.core/every?
cljs.core/not-any?
cljs.core/some
