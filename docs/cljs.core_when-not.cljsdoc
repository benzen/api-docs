===== Name
cljs.core/when-not

===== Type
macro

===== Signature
[test & body]

===== Description

Evaluates `test`. If logical false, evaluates `body` in an implicit `do`.

===== Example

===== Example

```
(when-not (odd? 12)
  "Twelve isnt odd")
;; => "Twelve isnt odd"

(when-not (even? 12)
  "Twelve isnt even")
;; => nil

```

===== Related
cljs.core/when
cljs.core/when-let
special/if
