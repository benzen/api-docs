===== Name
cljs.core/if-not

===== Type
macro

===== Signature
[test then]
[test then else]

===== Description

If `test` is false or nil, evaluates and returns `then`. Otherwise, evaluates
and returns `else`. `else` defaults to nil if not provided.


===== Example#c35976

```
(if-not (even? 1) "this is odd")
;; => "this is odd"

(if-not nil 1 -1)
;; => 1

(if-not (= 1 1) "whoops")
;; => nil
```

===== Related
special/if
cljs.core/when-not
